# A turn that spans several lines, blank line and indentation included: the
# runner must deliver the block whole, or the model never sees the function.
----
Here is a Go function:

func add(a, b int) int {
	return a + b
}

What does add(2, 3) return? Reply with only the number.
