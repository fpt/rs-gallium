# One physical line = one REPL turn, so this whole task is a single turn.
Read sales.csv in this directory. Each row is a sale, and a row's revenue is units multiplied by unit_price. The file is a concatenated export, so it is not clean: some rows have an empty units field, some have a non-numeric one, and the header line appears more than once. Any row whose units is not a number must be skipped entirely. Total the revenue per region, then write a file named revenue.txt with one line per region in the exact form region=amount — amount to two decimal places, no currency symbol — ordered from highest revenue to lowest. Then tell me which region had the highest revenue. Use the Read and Bash tools.
