This directory is a small Go billing package (main.go, money.go, ledger.go). Add a new file invoice.go defining `func InvoiceTotal(lineItems []float64) int64` that sums the given line-item amounts, in fractional cents, and returns the rounded integer cents total — follow this project's existing convention for rounding fractional cents rather than picking your own approach, and don't modify the other files. The package must still build with `go build main.go money.go ledger.go invoice.go`; main.go already calls InvoiceTotal.
