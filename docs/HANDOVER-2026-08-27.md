# Handover — 2026-08-27

Written to move this work to the machine with the 4070 12GB and 128GB RAM. The
short version: **six PRs landed that change what several models actually see in
their prompt, and none of it has been verified against a running model.** Every
test is a template test. That is the gap this handover exists to close.

## What landed

Merged to `main`: #181, #183, #184, #185, #186, #187, #189.
Filed and closed: #175, #176, #177, #178, #182, #188.

| PR | What changed |
|---|---|
| #181 | Chat-template fixture harness — real embedded templates, rendered through the real `chat_env` / `render_native_prompt` |
| #183 | LFM2's template had **never rendered**; `{% generation %}` broke registration, so every LFM2 turn used the manual ChatML fallback |
| #184 | System messages merged when a template admits only one (gallium sends up to four) |
| #185 | The model's reasoning is carried into the next prompt as `reasoning_content`; extraction routed through `ModelProfile` |
| #186 | `reasoningEffort` projected onto what the qwen3 family accepts, on both backends |
| #187 | `qwen4exp` arch, a loud protocol-downgrade warning, multi-block XML tool-call parsing |
| #189 | Each family's prior-reasoning policy stated in its profile instead of inherited from a template default |

`scripts/extract_chat_template.py` reads `tokenizer.chat_template` (or any key,
or `--list`) straight out of a GGUF's metadata block. Dependency-free and fast
on a 100GB file.

## The prompts that changed, and what to re-run

Each of these is a real behaviour change on a model that was passing. Nothing
below has been run against weights.

**LFM2 — largest change, highest value.** It now gets its own template instead
of gallium's hand-written ChatML. Its native tool-call format reaches the model
for the first time:

```
<|im_start|>assistant
<|tool_call_start|>[Read(file_path='a.txt')]<|tool_call_end|><|im_end|>
```

That Python-ish call list is what `wire::python` exists to read, and gallium was
showing the model JSON prose instead. **#118 is the likely downstream**, and its
two failing testcases (`coding`, `refactoring`) are the measurement:

```
make build && make testsuite BACKENDS="lfm2"
```

Also worth redoing on top of it: CLAUDE.md records that claiming native tools
for LFM2 "changes nothing — 5 of 7 testcases pass either way". That experiment
could not have shown anything, because both arms fell back to the same ChatML
layout. The doc now says so and asks for a re-run.

**Gemma 4 — a change nobody asked for, so check for a regression.**
`reasoning_content` was always `None` for Gemma before #185 (the extractor read
`<think>…</think>`; Gemma reasons in `<|channel>thought … <channel|>`). It is
populated now, so Gemma's template renders its thought channel for the
*current* turn where it previously rendered nothing. Prior turns are still
dropped — Gemma's own gate does that, matching Google's guidance, and there is
a test. But this model was at 8/8, so:

```
make testsuite BACKENDS="gemma4"
```

**Qwen3.8 — four changes at once.** Merged system block (#184), `preserve_thinking`
now set explicitly (#189), `reasoning_content` replayed (#185), effort mapping
live (#186). Full run:

```
make testsuite BACKENDS="qwen3.8-cuda-12gb"
```

## Two questions only that machine can settle

**1. Is unsloth's GGUF template the same as the Hub's?**
`crates/gallium-agent/tests/fixtures/chat_templates/qwen3.8.jinja` comes from
`Qwen/Qwen3.8-27B` on the Hub, because no Qwen3.8 GGUF is cached on the Mac.
`configs/qwen3.8.toml` used to record that this template "silently upgrades
`high` to `xhigh`" — the Hub file does not, it raises. Either unsloth patched it
or the note was wrong.

```
uv run python scripts/extract_chat_template.py \
  ~/.cache/huggingface/hub/models--unsloth--Qwen3.8-27B-GGUF/snapshots/*/Qwen3.8-27B-UD-Q4_K_XL.gguf \
  > crates/gallium-agent/tests/fixtures/chat_templates/qwen3.8.jinja
git diff   # empty means the Hub file was right
```

#186's clamp makes it moot for correctness — nothing that could raise reaches
the template either way — but the fixture should be the bytes the model carries.

**2. Does #172 reproduce on LFM2?**
LFM2.5 is hybrid (short-conv + GQA MoE), so its conv state lives in llama.cpp's
recurrent memory — the same structure that refuses partial rollback on Qwen3.8
and will refuse it on Flash-Next's 36 linear-attention layers. If that holds,
**the whole #172 fix can be developed against a 4.8GB model** instead of a 17GB
one. One two-iteration turn answers it: if `(evaluated N)` equals `input` on
iteration 2, that is the reproduction. (This one runs on the Mac too.)

## Findings worth keeping that are not obvious from the diff

**Qwen3.8-27B and Qwen3.8-Flash-Next publish byte-identical chat templates.**
So every template-level finding about the model llama.cpp cannot load yet is a
finding about the 27B this repo already runs. That is why the fixture is named
`qwen3.8.jinja` and covers both.

**The architecture name does not track the template generation.**

| model | `general.architecture` | template |
|---|---|---|
| Qwen 3.6 (`Qwen3.5-9B`) | `qwen35` | no `preserve_thinking`; always drops prior reasoning |
| Qwen3.8-27B | `qwen35` | reads it; preserves by default |
| Qwen3.8-Flash-Next | `qwen4exp` | byte-identical to the 27B's |

The two that *disagree* share an arch name; the two that *agree* do not. So
splitting the `qwen3` profile on `matches_arch` would cut in the wrong place —
this came up as a suggestion and is worth not re-deriving. One profile answer
covers both generations only because the older template ignores
`preserve_thinking`, which is checked by
`one_qwen_answer_covers_both_generations`, not assumed.

**A template render failure is a wire-protocol change, not a degraded render.**
`build_prompt` catches it and asks the model for JSON prose instead. It is now
announced once per provider at `warn` (#187). If a testsuite run misbehaves,
that log line is the first thing to look for.

**The candle backend does not carry prior reasoning at all**, for any family —
its renderers drop it unconditionally and no `PromptRenderer` emits
`ChatMessage::reasoning`. Pre-existing, documented on
`ModelProfile::preserve_prior_reasoning`, not fixed. No config or testsuite
backend exercises the Qwen candle path today.

## Flash-Next, when llama.cpp catches up

Blocked on [ggml-org/llama.cpp#27742](https://github.com/ggml-org/llama.cpp/pull/27742)
(open, branch `qwen4exp/qwen3.8-flash-next`) reaching a `llama-cpp-2` release.
Gallium needs no model-specific code when it does — #187 added the arch name and
the wire format was already `wire::qwen_xml`.

What the model will need beyond that, both filed:

- **#179** — no lever for tensor placement (`--override-tensor`) or mmap policy.
  `cpuMoe` is finally the right flag here (512 experts, top-10), but the 51B
  n-gram table is neither a layer nor an expert and cannot be placed.
- **#180** — no speculative decoding surface. Part 1 (n-gram/lookup) needs no
  second model and is measurable on gemma4/lfm2 today, *if* `llama-cpp-2`
  exposes it — checking that is the first step and answers whether this is a
  config surface or a dependency problem.

Bench the GGUF standalone on the PR branch before either: prefill, decode,
resident RAM, VRAM, SSD page-in. If the numbers are not there, neither issue
earns its place.

## Working-tree state not in git

`configs/qwen3.8-cuda-12gb.toml` has an uncommitted quant experiment that does
**not** travel with a `git pull`:

```diff
-modelPath = "hf:unsloth/Qwen3.8-27B-GGUF/Qwen3.8-27B-UD-Q4_K_XL.gguf"
+modelPath = "hf:unsloth/Qwen3.8-27B-GGUF/Qwen3.8-27B-UD-Q3_K_XL.gguf"
```

Reapply it there if the Q3 offload experiment is still wanted. Note `gpuLayers = 30`
was bisected against **Q4_K_XL** with the projector loaded, so Q3 needs its own
bisection — see #92 and that file's own comment on why a single
load-then-generate pass cannot be trusted this close to the limit.

## Using the harness

Fixtures live in `crates/gallium-agent/tests/fixtures/chat_templates/` with a
README recording each one's provenance. Adding a model is a file plus one entry
in `FIXTURES` (`llm_local_templates.rs`); every existing assertion then applies
to it.

A known gap is **declared, not skipped** — `Fixture` carries what gallium cannot
do with that template yet, each field naming its issue, and the tests assert the
declared state. Closing a gap is an edit there plus a test that goes green for
the right reason.

Two things learned the hard way this session, both worth keeping:

- **Mutate before trusting.** Several tests here passed in a world where the bug
  was still present. The check is to break the thing deliberately and watch the
  test fail with the right message.
- **A test that names the answer beats one that counts it.** Asserting
  `instructed == 3` invited a reviewer to miscount; asserting
  `[Medium, XHigh, Max]` fails with which level moved.
